# Convenience CLI configuration.
#
# Public site content and build directories are defined in content/config.yaml.
# This file only controls local site.py behavior.

project:
  name: "Legacy Defenders"
  version: "1.0.0"
  description: "Static site for inherited-home estate transition support"

development:
  server:
    host: "localhost"
    port: 8000

performance:
  minify_css: true
  lighthouse_min_score: 90

logging:
  level: "INFO"
